#ifndef LIBFT_H
# define LIBFT_H

# include <stdio.h>
# include <unistd.h>
# include <string.h>
# include <stdlib.h>

size_t	ft_strlen(char *a);
int		ft_strncmp(const char *s1, const char *s2, size_t n);

#endif
